// ⚠️ IMPORTANT: Replace this with your deployed backend URL before building
// for production (e.g. https://djs-cafe-api.onrender.com/api).
// Leaving this as localhost will break the live site — the browser
// will try to call localhost on the CLIENT's machine, not your server.
export const environment = {
  production: true,
  apiUrl: 'https://djs-internet-backend.onrender.com/api'
};
